import React, {Component} from 'react';
import {
  SafeAreaView,
  Text,
} from 'react-native';

// Experiments
import {
  AnimatedBackground,
  PulseIndicator,
  RollingBall,
} from './components';

export default () => (
  <RollingBall />
)